# $accent = $mauve
# $accentAlpha = $mauveAlpha
$font = RobotoMono Nerd Font
$text = rgba(235, 219, 178, 1)
$textHex = EBDBB2
$background = rgba(60, 56, 54, 1)
$yellow = rgba(215, 153, 33, 1)
$red = rgba(204, 36, 29, 1)

# GENERAL
general {
  hide_cursor = true
}

# BACKGROUND
background {
  monitor =
  path = $HOME/Pictures/wallpaper.jpg
  blur_passes = 1
  color = $base
}

# TIME
label {
  monitor =
  text = $TIME
  color = $text
  font_size = 50
  font_family = $font
  position = 0, -390
  halign = center
  valign = top
}

# DATE
label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $text
  font_size = 14
  font_family = $font
  position = 0, -470
  halign = center
  valign = top
}

# USER AVATAR
# image {
#   monitor =
#   path = $HOME/.face
#   size = 100
#   border_color = $accent
#   position = 0, 75
#   halign = center
#   valign = center
# }

# INPUT FIELD
input-field {
  monitor =
  size = 300, 60
  outline_thickness = 4
  dots_size = 0.2
  dots_spacing = 0.2
  dots_center = true
  outer_color = $background
  inner_color = $background
  font_color = $text
  font_family = $font
  fade_on_empty = false
  placeholder_text = <span foreground="##$textHex"><i>󰌾 Logged in as </i><span foreground="##$textHex">$USER</span></span>
  hide_input = false
  check_color = $yellow
  fail_color = $red
  fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
  capslock_color = $yellow
  position = 0, -47
  halign = center
  valign = center
}
